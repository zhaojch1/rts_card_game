# 卡牌 RTS 战棋游戏 · 需求文档

## 文档目的

本文是《卡牌 RTS 战棋游戏》的**唯一权威需求文档**，面向开发、测试与项目决策，读者无需任何前置上下文。文档规定了：产品定位、技术选型、总体架构、垂直迭代开发路线、动画与美术规范、玩法设计、数据设计与验收标准。所有后续开发以此为准，如需变更走正式修订。

---

## 1. 项目概述

### 1.1 游戏定位
- **类型**：网页端卡牌 RTS 战棋游戏（自动战斗 + 卡牌策略）
- **核心玩法**：玩家从卡牌库组卡，在战场指定区域部署卡牌单位；单位入场后自动寻敌、移动、攻击；每回合已部署卡牌自动增长单位数量；消灭敌方全部单位获胜。
- **目标平台**：现代桌面与移动浏览器（Chrome、Firefox、Safari、Edge）。

### 1.2 技术栈（最终定稿）

| 领域 | 选择 | 说明 |
|---|---|---|
| 语言 | **TypeScript** | 强类型、利于长期维护与安全重构 |
| 构建 | **Vite** | 开发热更新快，构建产物现代 |
| 渲染 | **PixiJS v8（WebGL 渲染器）** | WebGL2/WebGPU 渲染，Canvas 回退；提供精灵批处理、纹理图集、滤镜与自定义 shader |
| 动画 | **DragonBones 运行时（PixiJS 适配）+ 程序化骨骼生成** | 骨骼动画；数据当前由程序化生成器产出，未来可替换为编辑器美术数据 |
| 单位形象 | **程序化矢量** | 纯代码绘制矢量部件，无外部美术资源依赖 |
| 运行时 | 纯前端 | 单机可完整演示对局，后端/云服务为后续扩展 |

### 1.3 核心理念
1. **精细化优先**：先追求单个兵种的动作与表现质量达标，再谈功能数量与广度。
2. **垂直迭代**：每次开发交付一个"从输入到画面到结算"的完整切片，而非半成品模块。
3. **验收门（Gate）**：每个阶段有明确的完成标准（Definition of Done），上一阶段不达标绝不进入下一阶段。
4. **一步一个脚印，尽量不返工**：通过架构隔离与验收门，从机制上压制返工；返工成本被限制在最小范围内。

---

## 2. 开发哲学与反返工原则

### 2.1 垂直切片定义
一个垂直切片 = 一个**可直接观察、可直接判断"满意/不满意"**的完整画面与行为。

本项目的第一张垂直切片（即首个里程碑）是：

> **一张完整的战场地图上，两个长枪兵自动对打：寻敌 → 接近 → 攻击 → 格挡 → 受击 → 死亡 → 一方倒下。**
>
> 这一画面的动作流畅度、打击感、特效、镜头**令人满意**，才标志地基成立，才允许进入下一兵种。

### 2.2 反返工六条铁律
1. **逻辑与渲染彻底解耦**：单位、AI、战斗、回合等一切游戏规则代码中，**禁止出现任何渲染 API 调用**（PixiJS / WebGL / Canvas 均不可）。渲染只通过 `IRenderer` 接口发生。
2. **渲染抽象层**：所有绘制收口到 `render/` 模块。将来若更换渲染底层，只改 `render/` 内部实现，游戏逻辑一行不动。
3. **动画数据驱动**：动画状态机、动作列表、骨骼关键帧全部来自数据，不硬编码在单位类。新增动作 = 加数据 + 加资源，不改代码结构。
4. **兵种数据驱动**：新增兵种 = 一份兵种配置 + 一套程序化矢量定义 + 少量特有逻辑。地基（渲染管线、动画系统、战斗循环）不因新增兵种而改动。
5. **固定时间步长**：逻辑模拟用固定 dt（1/60s），渲染独立插值。逻辑确定、可复现，且与渲染器、刷新率解耦。
6. **验收门**：每阶段有可执行完成标准，带着"方向不对的实现"继续走是返工的最大来源。

---

## 3. 技术选型详解

### 3.1 TypeScript
- 全程使用 TypeScript，开启严格模式（`strict: true`）。
- 游戏逻辑层（`sim/`）不依赖 DOM 与渲染类型，可独立单元测试。
- 共享类型定义（单位、卡牌、动画数据）集中在 `types/`，作为各模块契约。

### 3.2 PixiJS v8 渲染
- 使用 PixiJS v8 的 WebGL 渲染器；自动回退 Canvas 仅作兼容兜底，不改变渲染接口。
- 使用其能力：精灵（Sprite）、纹理图集（Spritesheet）、网格（Mesh）、滤镜（Filter）、自定义 shader、容器分层（Container）。
- 本项目为 2D 精灵 + 动画 + shader 特效，PixiJS 封装比裸写 WebGL 开发效率更高、坑更少，同时保留自定义 shader 的精细化空间。

### 3.3 程序化骨骼动画（核心创新点）
由于**当前无美术资源**，但需求上要骨骼动画的平滑混合与关键帧能力，采用如下闭环：

1. **程序化矢量部件**：每个兵种的身体部位（头、躯干、四肢、武器、装备）由代码用矢量图形（圆形、矩形、多边形、贝塞尔、渐变、描边）定义。
2. **程序化装配（Rigging）**：代码定义骨骼层级（根 → 骨盆 → 躯干 → 头；左右臂；左右腿；武器挂点），每个矢量部件绑定到骨骼。
3. **程序化烘焙**：矢量部件烘焙为纹理（图集），作为刚体部件挂到骨骼上（与 DragonBones 的"图片插槽"一致）；必要时对有机部位做网格蒙皮。
4. **关键帧动画**：每个动作（待机/移动/攻击/格挡/受击/死亡/转向）由代码定义骨骼关键帧序列（位移、旋转、缩放随时间变化）。
5. **DragonBones 运行时播放**：动画数据以 DragonBones 兼容格式组织，由 DragonBones 运行时（PixiJS 适配）负责状态机、混合与插值。
6. **未来美术替换**：当美术到位后，用 DragonBones/Spine 编辑器产出真实数据，**直接替换数据文件即可，动画运行时与游戏逻辑无需改动**——这是本方案的关键反返工设计。

> 结论：**"骨骼动画"与"程序化矢量无美术"并不冲突**——骨骼动画定义的是"动作如何驱动"，程序化矢量定义的是"部件长什么样"。二者通过 DragonBones 兼容数据格式解耦。

### 3.4 为什么不用裸写 WebGL
本项目是 2D 精灵 + 骨骼动画 + shader 特效，不需要深度自定义的 3D 管线。裸写 WebGL 需手写顶点缓冲、着色器编译、纹理管理、context 丢失恢复等样板，成本高且坑多。PixiJS 已覆盖所需能力，并允许自定义 shader 满足精细化特效。渲染层被 `IRenderer` 抽象，未来若确有需要仍可平滑替换。

---

## 4. 总体架构

### 4.1 目录结构
```
rts_card_game/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── src/
│   ├── core/                 # 主循环、固定时间步长、事件、资源加载
│   │   ├── loop.ts           #   模拟(固定dt) + 渲染(插值) 分离
│   │   ├── clock.ts          #   时间与 delta 计算
│   │   └── loader.ts         #   资源加载与缓存
│   ├── render/               # 渲染抽象（唯一接触渲染 API 的模块）
│   │   ├── IRenderer.ts      #   渲染接口
│   │   ├── PixiRenderer.ts   #   PixiJS v8 实现
│   │   ├── camera.ts         #   世界坐标↔屏幕坐标、平移缩放
│   │   ├── layers.ts         #   分层：背景/阴影/单位/特效/UI
│   │   ├── batch.ts          #   批处理封装
│   │   └── shaders/          #   自定义 shader（描边/闪白/溶解等）
│   ├── sim/                  # 纯逻辑模拟（禁止 import 渲染）
│   │   ├── unit.ts           #   单位状态与属性
│   │   ├── ai.ts             #   寻敌、移动、攻击决策
│   │   ├── combat.ts         #   伤害、克制、暴击
│   │   ├── battle.ts         #   战斗世界状态
│   │   ├── round.ts          #   回合与增长系统
│   │   └── spatial.ts        #   四叉树空间分区
│   ├── anim/                 # 动画系统（数据驱动，骨骼播放）
│   │   ├── stateMachine.ts   #   动画状态机
│   │   ├── dragonbones.ts    #   DragonBones 运行时封装
│   │   └── events.ts         #   动画事件（命中帧、收招等）
│   ├── art/                  # 程序化矢量与程序化骨骼生成
│   │   ├── vector.ts         #   矢量部件绘制原语
│   │   ├── parts/            #   各兵种部件定义
│   │   ├── rig.ts            #   骨骼层级与绑定
│   │   ├── bake.ts           #   矢量→纹理图集烘焙
│   │   └── generate.ts       #   矢量+骨骼→DragonBones 数据生成器
│   ├── game/                 # 场景组装：把 sim + anim + render 连接
│   │   ├── BattleScene.ts
│   │   └── scenes.ts
│   ├── types/                # 共享类型契约
│   ├── data/                 # 兵种/卡牌/动画配置
│   ├── ui/                   # 用户界面（DOM 或渲染 UI 层）
│   └── utils/                # 数学、对象池、通用工具
└── assets/                   # 运行时生成/烘焙产物、音频、shader 源
```

### 4.2 依赖方向（硬性约束）
```
types  ←  sim  ←  game
            ↑        ↑
           anim  ←  game
types  ←  render  ←  game
```
- `sim/` 不依赖 `render/`、不依赖 DOM。
- `anim/` 只依赖数据与 `types/`，不依赖 `sim/`。
- `render/` 只依赖接口，不依赖具体单位类型。
- `art/`（程序化生成）只依赖矢量原语与数据定义，产出可被 `anim/` 与 `render/` 消费的资源。
- 违反依赖方向即视为架构债，需立即修复。

### 4.3 数据流
```
data/ 配置 → art/ 生成矢量+骨骼数据 → 烘焙为纹理/骨骼数据
                                        ↓
sim/ 计算单位逻辑状态 → 产生"单位要播放哪个动画"的信号
                                        ↓
anim/ 状态机驱动骨骼播放 → 输出每帧骨骼变换
                                        ↓
render/ 将骨骼变换渲染为画面
```

---

## 5. 渲染系统

### 5.1 分层渲染
渲染按固定顺序分层，便于隔离与优化：

| 层 | 内容 | 说明 |
|---|---|---|
| 背景层 | 地形、草地、路径、装饰 | 静态或低频更新 |
| 阴影层 | 单位投影 | 贴地阴影，随单位位置移动 |
| 单位层 | 各单位身体、武器、动画 | 核心动态层 |
| 特效层 | 刀光、箭矢、火花、溶解、飘字 | 高频特效 |
| UI 层 | 血条、选中框、指示器 | 可独立于世界坐标 |

### 5.2 摄像机
- 世界坐标 ↔ 屏幕坐标矩阵变换。
- 平移 + 缩放 + 边界限制。
- 战斗中可跟随选中单位或关键目标。
- 缩放范围受限，保证单位始终清晰可见。

### 5.3 精细化 shader 特效
这是 WebGL 相对 Canvas 2D 的核心价值，用于提升"精细化"表现：

| 特效 | 用途 |
|---|---|
| 描边/外发光 | 选中单位、技能范围、稀有度高亮 |
| 受击闪白 | 受击帧将单位渲染为白色，突出打击感 |
| 溶解（死亡） | 噪声纹理驱动的边缘消散，替代"瞬间消失" |
| 渐变/光照/阴影 | 让程序化矢量单位有体积感与质感 |
| 颜色混合 | 己方蓝色系 / 敌方红色系 / 中立灰色系动态着色 |

### 5.4 批处理与性能
- 同纹理、同 shader 的对象合批，减少 draw call。
- 纹理图集合并，减少状态切换。
- 动态对象与静态对象分离，静态层只重绘一次。
- 对象池复用精灵/网格，减少 GC。
- 目标 60fps，低端设备自动降级（降特效、降帧率）。

---

## 6. 动画系统

### 6.1 动画状态机
每个单位有一个数据驱动的动画状态机，状态包括：

| 状态 | 状态名 | 触发 |
|---|---|---|
| 待机 | `idle` | 无目标、无移动 |
| 移动 | `walk` | 向目标移动 |
| 转向 | `turn` | 朝向变化 |
| 攻击 | `attack` | 进入攻击范围 |
| 格挡 | `block` | 受击时触发格挡 |
| 受击 | `hit` | 被命中 |
| 死亡 | `death` | 生命值归零 |

- 状态间支持中断切换与平滑混合（如攻击中途被要求格挡）。
- 状态机由配置驱动，动作名与数据对应，不硬编码。

### 6.2 关键帧与骨骼
- 每个动作是一段骨骼关键帧序列，包含每根骨骼的位移/旋转/缩放随时间变化。
- 关键帧曲线支持缓动（easing），保证动作自然。
- 动画事件（如"命中帧"）随关键帧标注，驱动游戏逻辑（如伤害结算时机）。

### 6.3 动画事件与逻辑联动
- 攻击动作在"命中帧"事件触发伤害结算，而非动画结束才结算，保证画面与数值同步。
- 格挡成功/失败由逻辑判断后回传动画，播放不同反馈。

---

## 7. 单位形象系统（程序化矢量）

### 7.1 矢量部件原则
- 所有单位由**矢量部件**组合而成，纯代码绘制，无外部图片。
- 每个部件由几何图形（圆形、矩形、多边形、贝塞尔曲线）与填充、渐变、描边构成。
- 部件有明确的**图层顺序**（如：后背 → 身体 → 前臂 → 武器 → 头部）。
- 部件有**绑定原点（pivot）**，用于挂接到骨骼并随骨骼变换。

### 7.2 程序化装配（Rigging）
- 每个兵种定义一套骨骼层级，矢量部件绑定到骨骼。
- 常见骨骼层级（以人形为例）：
  - 根 → 骨盆 → 躯干 → 头
  - 躯干 → 左臂/右臂（上臂 → 前臂 → 手）
  - 骨盆 → 左腿/右腿（大腿 → 小腿 → 脚）
  - 手 → 武器挂点
- 部件默认绑定单根骨骼作为刚体（与 DragonBones 图片插槽一致）；有机部位可扩展为网格蒙皮。

### 7.3 程序化烘焙
- 矢量部件绘制后烘焙为纹理图集（一次性，运行期缓存），供 PixiJS 渲染。
- 烘焙产物（图集 + 骨骼数据）由 `art/generate.ts` 生成，符合 DragonBones 数据格式。
- 未来美术替换：以编辑器产出的图集 + 骨骼数据**同名替换**即可，运行时无改动。

### 7.4 兵种形象草案（以长枪兵为例）
- 头部：圆形 + 头盔细节（渐变金属）。
- 身体：梯形铠甲，带描边与渐变。
- 武器：长枪，金属渐变杆 + 枪头。
- 腿部：两个矩形，表现迈步。
- 组合表现：方阵排列、队形移动。

（弓箭手、剑士、骑兵等兵种形象在对应阶段细化，遵循同一部件规范。）

---

## 8. 垂直迭代开发路线

> 原则：每个阶段都是可体验、可验收的切片；上一阶段验收不通过，不进入下一阶段。

### 阶段 0 —— 地基：工程 + 渲染管线 + 主循环
- **目标**：TypeScript 工程脚手架、PixiJS v8 渲染管线、固定时间步长主循环、`IRenderer` 抽象。
- **交付物**：屏幕上出现可被 shader 着色的测试对象，以 60fps 稳定运动，逻辑与渲染分离就位。
- **验收标准**：
  1. 固定时间步长主循环运行，模拟与渲染解耦。
  2. 测试对象正常显示，shader 参数可调。
  3. 不同刷新率（如 144Hz）下逻辑速度一致。
  4. 代码满足第 4 章依赖方向约束。
  5. `art/generate.ts` 能生成一个最小骨骼数据并被 DragonBones 运行时加载。

### 阶段 1 —— 战场地图与镜头
- **目标**：一张"像战场"的地图 + 可平移缩放摄像机 + 单位贴地坐标。
- **交付物**：草地/路径/地形分层渲染，摄像机平移缩放，单位正确投影到地面。
- **验收标准**：
  1. 地图分层渲染正确（地面、阴影、可通行区）。
  2. 摄像机平移/缩放流畅。
  3. 世界坐标 ↔ 屏幕坐标换算正确，缩放时单位与地面不脱节。

### 阶段 2 —— 长枪兵（单兵全动作）★ 精细化主战场
- **目标**：一个长枪兵具备全套高质量动画：待机 / 移动 / 攻击 / 格挡 / 受击 / 死亡 / 转向。
- **交付物**：单兵可循环播放所有动作，切换平滑，有受击闪白、飘字、死亡溶解等反馈。
- **验收标准**：逐条核对第 9 章《长枪兵动画规范》。

### 阶段 3 —— 两个长枪兵对打（首个验收门 ★★★）
- **目标**：本项目第一张完整垂直切片——一张地图上两个长枪兵自动对打直到一方死亡。
- **范围**：寻敌 → 接近 → 攻击 → 格挡 → 受击 → 死亡 → 结算，全链路。
- **验收标准**：
  1. 两单位自动寻敌、移动、攻击，逻辑正确。
  2. 攻击与格挡真实交互（非各打各的）：格挡成功有格挡反馈，格挡失败有受击反馈。
  3. 打击感到位：前摇、命中顿帧、受击闪白、轻微震屏、飘字、死亡溶解。
  4. **整体画面"令人满意"——这是进入下一阶段的主观硬门槛。**
- **说明**：此阶段不满意，就在阶段 2/3 内打磨动作与反馈，**不新增任何兵种**。

### 阶段 4 —— 第二个兵种（如弓箭手）★ 验证可扩展性
- **目标**：新增兵种，验证"新增兵种不改地基"是否成立。
- **交付物**：弓箭手全套动作 + 远程攻击（箭矢飞行、命中）。
- **验收标准**：
  1. 新增兵种只改 `data/` 配置 + `art/parts/` 矢量定义 + 少量特有逻辑，**未改动渲染管线与战斗循环骨架**。
  2. 若被迫大改地基，说明阶段 0–3 抽象有缺陷，**回退修复抽象，而非硬塞**。

### 阶段 5 —— 从小队对打扩展到多单位战斗
- **目标**：多单位、阵型、协同、批量性能。
- **范围**：同卡牌多单位（如 10 个长枪兵方阵）、阵型维持、四叉树碰撞、批量渲染。
- **验收标准**：
  1. 单场 200 单位稳定 60fps。
  2. 阵型移动不重叠、不穿帮。

### 阶段 6 —— 卡牌 / 部署 / 回合系统
- **目标**：把"对打"扩展为"卡牌战棋"。
- **范围**：卡牌数据、卡组、部署区、能量费用、回合自动增长（每回合 +N 单位）、胜负结算。
- **验收标准**：见第 10、11、12 章对应条目，按功能逐个验收。

### 阶段 7 —— UI / 结算 / 音效 / 优化
- **目标**：主界面、战斗界面、结算界面、音效与 BGM、性能与内存优化。
- **验收标准**：见第 15 章成功指标。

> **路线总原则**：阶段 0–3 把一件事做深做透；阶段 4 验证模式可复制；阶段 5 之后才广度展开。顺序不可颠倒。

---

## 9. 长枪兵动画规范（首个兵种精细规格）

> 本章是阶段 2 的验收依据，也是后续所有兵种动画的模板。

### 9.1 动作清单（全部必须达标）

| 动作 | 状态名 | 关键要求 |
|---|---|---|
| 待机 | `idle` | 呼吸/重心微晃，持枪姿态稳定，不僵硬 |
| 移动 | `walk` | 步伐与重心起伏，长枪随身体自然摆动 |
| 转向 | `turn` | 朝向平滑插值，改变方向不瞬移 |
| 攻击 | `attack` | 三阶段：前摇（前踏蓄力）→ 命中（出枪刺击）→ 收招（收枪回位）；含明确"命中帧"事件 |
| 格挡 | `block` | 举枪/横枪格挡，格挡命中时枪体受力偏移 |
| 受击 | `hit` | 身体后仰、短暂硬直、闪白反馈 |
| 死亡 | `death` | 倒地 + 溶解消散过渡，不突然消失 |

### 9.2 打击感清单（阶段 3 主观验收关键）
1. **前摇（Anticipation）**：攻击前有可感知蓄力帧，不瞬间出伤害。
2. **命中顿帧（Hitstop）**：命中瞬间双方短暂停顿数帧，制造"打中了"的重量感。
3. **受击反馈**：受击单位后仰 + 闪白 + 飘字（伤害数字）。
4. **屏幕震动**：命中/死亡时轻微震屏（可开关、可调幅度）。
5. **格挡反馈**：格挡成功出现区别于受击的反馈（枪体偏移、火花、音效），"挡没挡住"一眼可辨。
6. **死亡表现**：倒地 + 溶解，而非瞬间消失。

### 9.3 动作衔接规则
- 所有状态间可中断切换（如攻击中途被要求格挡），切换平滑混合，不跳帧。
- 动作由 `anim/stateMachine.ts` 数据驱动，动作名与配置对应（如 `spearman_attack`），不硬编码。

---

## 10. 核心玩法

### 10.1 游戏流程
1. **准备阶段**：从卡牌库选卡组卡。
2. **部署阶段**：在指定区域放置卡牌（每张卡牌部署一组单位）。
3. **战斗阶段**：单位自动寻敌、攻击、移动。
4. **回合阶段**：每回合开始，已部署卡牌自动增加单位（如每回合 +2 长枪兵），增长量受卡牌等级影响。
5. **结算阶段**：判定胜负，发放奖励。

### 10.2 卡牌系统
**卡牌属性**：
- 名称、等级（1–5 星）、费用（部署能量/资源）
- 单位组合（类型与数量，如 10 长枪兵，或 5 刀盾兵 + 3 弓箭手）
- 单位属性（攻击力、生命值、防御力、攻击速度、移动速度、攻击范围）
- 特殊技能（冲锋、治疗、AOE 等）
- 升级效果（每级增加单位数量与属性）
- 回合增长（每回合自动增加的单位数量）

**卡牌分类**：
- 步兵类：长枪兵连、剑士小队
- 远程类：弓箭手小队、弩手编队
- 骑兵类：轻骑兵队、重骑兵排
- 混合类：刀盾兵弓箭手组合
- 特殊类：治疗小队、法师团
- 攻城类：投石车编队、攻城器械队

### 10.3 战斗规则
- **自动寻敌**：寻找最近敌方单位。
- **攻击逻辑**：进入攻击范围后自动攻击。
- **移动逻辑**：向目标移动，保持阵型。
- **仇恨机制**：优先攻击正在攻击自己的单位。
- **协同行为**：同卡牌单位保持队形，协同移动与攻击。
- **阵型维持**：移动时保持相对位置，避免重叠。

### 10.4 伤害计算
- 基础伤害 = 攻击力 − 防御力
- 暴击：概率触发 1.5 倍伤害
- 类型克制：步兵克骑兵、骑兵克远程、远程克步兵

### 10.5 胜负条件
- 胜利：消灭所有敌方单位。
- 失败：己方所有单位被消灭。
- 时间限制：可选战斗时间上限。

---

## 11. 战斗系统设计

### 11.1 单位 AI
- 状态：待机 → 索敌 → 追击 → 攻击 → 返回（目标死亡后重新索敌）。
- 索敌优先级：攻击自己的单位 > 最近单位。
- 远程单位保持距离，近战单位贴脸攻击。

### 11.2 阵型与协同
- 同卡牌单位以预设阵型（方阵/横排/前后排）生成与移动。
- 移动时保持相对位置，用局部避让防止重叠。
- 混合卡牌（如刀盾兵 + 弓箭手）前排近战、后排远程协同。

### 11.3 空间分区与碰撞
- 四叉树管理单位空间查询，降低碰撞检测复杂度。
- 单位间使用圆形/矩形近似碰撞体。
- 目标 200 单位时碰撞与索敌不成为性能瓶颈。

### 11.4 战斗时序
- 固定时间步长驱动，逻辑确定。
- 攻击命中由动画"命中帧"事件驱动伤害结算，保证画面数值同步。

---

## 12. 用户界面

### 12.1 主界面
- 卡牌选择区（显示单位组合、数量、等级），支持筛选、搜索、排序。
- 卡组配置区（拖拽调整、组合预览）。
- 卡牌详情面板（单位类型、数量、升级效果、回合增长）。
- 卡牌升级 / 删除 / 锁定 / 收藏 / 组合推荐。
- 资源显示、开始战斗按钮、设置按钮。

### 12.2 战斗界面
- 战场区域（渲染系统绘制）。
- 单位信息面板（选中单位/卡牌详情）。
- 资源条（部署与升级用能量）。
- 卡牌手牌区（名称、单位组合、当前数量）。
- 回合指示器（当前回合、增长状态、下回合增加预览）。
- 控制按钮（暂停、加速、退出）。

### 12.3 结算界面
- 战斗结果（胜/负）。
- 奖励显示（资源、卡牌）。
- 统计信息（伤害输出、存活单位、卡牌使用情况）。

---

## 13. 数据设计

### 13.1 卡牌数据（JSON）
```json
{
  "id": "spearman_squad",
  "name": "长枪兵连",
  "description": "基础步兵单位组合，擅长对抗骑兵",
  "cost": 5,
  "unitType": "spearman",
  "unitCount": 10,
  "unitStats": {
    "attack": 15, "health": 100, "defense": 5,
    "attackSpeed": 1.0, "moveSpeed": 1.2, "attackRange": 2.0
  },
  "type": "infantry",
  "rarity": "common",
  "skills": ["charge"],
  "upgradeEffect": {
    "unitCountIncrease": 2,
    "attackIncrease": 10,
    "healthIncrease": 15
  },
  "roundGrowth": { "unitCountPerRound": 2, "maxUnits": 20 }
}
```

### 13.2 程序化矢量 + 骨骼定义（新增，驱动动画）
```json
{
  "unitType": "spearman",
  "parts": [
    { "id": "head", "layer": 10, "shapes": ["circle", "helmet"], "bindBone": "head" },
    { "id": "body", "layer": 5, "shapes": ["torso_armor"], "bindBone": "torso" },
    { "id": "spear", "layer": 20, "shapes": ["spear_shaft", "spear_head"], "bindBone": "weapon" }
  ],
  "skeleton": [
    { "id": "root", "parent": null },
    { "id": "torso", "parent": "root" },
    { "id": "head", "parent": "torso" },
    { "id": "weapon", "parent": "hand_r" }
  ],
  "animations": {
    "idle":   { "duration": 2.0, "tracks": {} },
    "walk":   { "duration": 1.0, "tracks": {} },
    "attack": { "duration": 0.8, "tracks": {}, "events": [{ "time": 0.4, "name": "hit_frame" }] },
    "block":  { "duration": 0.5, "tracks": {} },
    "hit":    { "duration": 0.4, "tracks": {} },
    "death":  { "duration": 1.5, "tracks": {} },
    "turn":   { "duration": 0.2, "tracks": {} }
  }
}
```
> 矢量部件（`parts`）、骨骼（`skeleton`）、动画（`animations`）由 `art/generate.ts` 解析并生成 DragonBones 兼容数据与纹理图集。

### 13.3 等级成长
- 单位数量增长：每升一级增加单位数量（如长枪兵连每级 +2）。
- 属性增强：每级攻击力 +10%、生命值 +15%。
- 等级上限 5 级；升级消耗相同卡牌或通用经验卡。

---

## 14. 性能优化

| 手段 | 说明 |
|---|---|
| 对象池 | 复用单位/精灵/网格对象，减少 GC |
| 空间分区 | 四叉树优化碰撞与索敌 |
| 批量渲染 | 同纹理/同 shader 合批，减少 draw call |
| 纹理图集 | 合并小纹理，减少状态切换 |
| 分层渲染 | 静态层只画一次，动态层增量更新 |
| 帧率控制 | 目标 60fps，自动降级策略 |
| 单位分组 | 同卡牌单位共享渲染逻辑，减少状态切换 |

---

## 15. 风险与应对

| 风险 | 说明 | 应对 |
|---|---|---|
| 精细化打磨拖慢进度 | 在单兵种上无限打磨 | 用第 9 章动作清单 + 打击感清单作客观验收线，达标即过 |
| 验收门被主观放宽 | "差不多就行"导致带病前进 | 阶段 3 的"画面满意"明确、可复查；记录不满意点逐条关闭 |
| 程序化骨骼复杂度 | 无美术时骨骼数据靠代码生成，调试成本高 | 阶段 0 先打通最小骨骼数据生成与播放闭环；参数化调整 |
| 新增兵种被迫改地基 | 抽象不足导致返工 | 阶段 4 作为"可扩展性验收"，发现即回退修复抽象 |
| 性能 | 大量单位/特效 | WebGL 批处理 + 对象池 + 四叉树 + 自动降级 |
| 浏览器兼容 | 不同浏览器 WebGL 实现差异 | PixiJS 统一封装 + 回退策略 + 目标浏览器测试 |

---

## 16. 成功指标

### 16.1 阶段性指标（按验收门）
- **阶段 3 达成** = 两个长枪兵对打画面"令人满意"（第 9 章全部通过）。
- **阶段 4 达成** = 新增弓箭手未改动地基。
- **阶段 5 达成** = 200 单位稳定 60fps。

### 16.2 整体指标
- 帧率：200 单位时稳定 60fps。
- 加载：首屏加载 < 3 秒。
- 内存：< 512MB。
- 规模：单场战斗最多 200 单位。
- 卡牌多样性：≥ 15 种卡牌（含不同单位组合）。
- 单位类型：≥ 6 种基础单位类型。
- 平衡性：卡牌胜率 45%–55%。
- 时长：平均单局 3–5 分钟。

---

## 17. 未来扩展

- **多人对战**：WebSocket 实时对战。
- **战役模式**：PVE 关卡挑战。
- **卡牌收集**：抽卡、升级系统。
- **公会系统**：玩家社区功能。
- **赛季排名**：竞技排名系统。
- **美术资产替换**：程序化矢量无缝替换为美术制作的 DragonBones/Spine 骨骼资产（数据同名替换）。
- **物理引擎**：更真实碰撞（可选）。
- **数据持久化**：云存储玩家进度。

---

## 附：术语表

| 术语 | 含义 |
|---|---|
| 垂直切片 | 从输入到画面到结算的完整可体验功能单元 |
| 验收门（Gate） | 阶段完成标准，不达标不进入下一阶段 |
| DoD | Definition of Done，完成定义 |
| 程序化矢量 | 用代码绘制矢量图形生成单位形象 |
| 程序化骨骼 | 用代码定义骨骼层级与关键帧动画 |
| 烘焙 | 将矢量部件一次性转为纹理图集的过程 |
| Hitstop | 命中瞬间的短暂停顿，增强打击感 |

---

*本文档为最终权威需求文档，后续开发、测试、验收均以此为准。进入每个阶段前，可依据本文拆解该阶段的细化任务，但不得与本文的核心约束（第 2、4 章）冲突。*
