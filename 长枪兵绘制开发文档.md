# 长枪兵绘制开发文档（外包任务）

> 本任务是《卡牌 RTS 战棋游戏》项目中"长枪兵单位形象"的程序化矢量绘制改进。
> 阅读本文档即可开工；如需项目上下文可再阅读仓库内 `需求文档.md`（第 2、4、7、9 章）与下述指定文件。

---

## 0. 任务一句话

把当前"四根筷子"级别的长枪兵矢量部件，重绘成**有体积感、有关节、有明暗层次**的写实战棋风单位（约 72–77 像素高的侧视持枪步兵），并保证不改动渲染/动画/逻辑框架。

---

## 1. 项目背景

- 网页端卡牌 RTS 战棋游戏，TypeScript + Vite + PixiJS v8。
- 所有单位形象**纯代码绘制**（Canvas 2D 矢量图形），**禁止使用外部图片资源**。
- 矢量部件被"烘焙"成纹理图集，挂接到程序化骨骼上播放动画（待机/移动/攻击/格挡/受击/死亡/转向）。
- 因此你的工作 = 改进**部件画什么**；骨骼、动画、渲染都已被框架处理，不需要你动。

---

## 2. 职责范围

### 只允许修改的文件

| 文件 | 作用 | 允许改动 |
|---|---|---|
| `src/art/parts/spearman.ts` | **本次主战场**：长枪兵全部矢量部件绘制 | ✅ 完全重写绘制函数 |
| `src/art/vector.ts` | Canvas2D 绘制原语库 | ✅ 可新增原语（如弧线、星形、阴影辅助），不得破坏现有导出签名 |
| `src/art/rig.ts` | 骨骼层级与 rest 姿态 | ⚠️ 见 §6：默认**禁止**；确有必要时按 §6 规则微调 |

### 禁止修改

- `src/sim/**`（战斗逻辑）、`src/anim/**`（动画运行时）、`src/render/**`（渲染框架）、`src/data/**`（属性/卡牌）
- `src/art/generate.ts`（动画关键帧数据；除非你改了 rest 姿态导致某动作穿帮，且需在交付说明中逐条列出）
- `src/art/bake.ts`（烘焙流程）、任何测试与脚本

---

## 3. 你必须知道的技术约定（重要）

### 3.1 坐标系

- 每个部件以**骨骼挂点**为原点 `(0,0)` 绘制，**y 向下为正**（与屏幕一致）。
- 部件画在独立的画布上，超出画布会被裁剪 → 改大形状时必须同步增大 `PartDef` 的 `w/h`。
- 单位总高约 **72–77px**：`root` 在脚底（y=0），身体向上（负 y 方向），盔顶约 y=-72..-77（现状头偏大，本次应收到 14–16px，见 §5.1）。

### 3.2 骨骼层级与 rest 姿态（部件挂在哪、默认朝哪）

```
root (0,0)                         ← 脚底
└─ pelvis (0,-22)                  ← 髋
   ├─ torso (0,-18)  → 世界 -40    ← 肩/颈
   │  ├─ head (0,-18)  → 世界 -58  ← 头骨（部件向上画 14–16px 到头/盔顶）
   │  ├─ arm_l (7,-12, rot +0.15)  ← 左肩（画面右侧，前臂）
   │  │  └─ hand_l (0,12)          ← 左肘→左手（世界约 -52..-28，胸前高度）
   │  └─ arm_r (-7,-12, rot -0.15) ← 右肩（画面左侧，后臂，持枪）
   │     └─ hand_r (0,12)
   │        └─ weapon (0,0, rot -0.2)  ← 长枪挂点（右手，世界约 (-7,-40)，胸前）
   ├─ leg_l (-5,0)                 ← 左髋（画面右侧）
   │  └─ foot_l (0,22)             ← 左踝（世界 y=0）
   └─ leg_r (5,0)
      └─ foot_r (0,22)
```

- 单位面向 **+x**（画面右）；攻击方向为 +x。
- 部件坐标系原点即骨骼原点：如腿部件原点在髋关节，腿向下延伸 22px 到踝；脚部件原点在踝，向下延伸 5px 到脚底。

### 3.3 部件层序（displayIndex，越小越先绘制 = 越靠后）

```
leg_r(0) → leg_l(1) → foot_r(2) → foot_l(3) → arm_r(4) → hand_r(5)
→ torso(6) → head(7) → arm_l(8) → hand_l(9) → weapon(10)
```

即：先画双腿双脚，再画右臂（背后的持枪臂），再画躯干、头，然后左臂（前面的臂），最后武器在最上层。

### 3.4 部件清单与当前画布尺寸（改形状需同步更新 w/h）

| 部件名 | 挂点 | 当前画布 w×h | 当前用途 |
|---|---|---|---|
| `head` | 头骨 (0,0) | 20×22 | 头+盔 |
| `torso` | 骨盆顶部 (0,0) | 22×20 | 躯干（向上延伸 18px 到颈） |
| `arm_l` / `arm_r` | 肩关节 (0,0) | 12×16 | 上臂（向下 12px） |
| `hand_l` / `hand_r` | 肘 (0,0) | 10×15 | 前臂+手（向下 12px） |
| `leg_l` / `leg_r` | 髋 (0,0) | 12×20 | 大腿+小腿（向下 18px，接脚） |
| `foot_l` / `foot_r` | 踝 (0,0) | 12×8 | 靴子（向下 5px） |
| `weapon` | 右手 (0,0) | 62×12 | 长枪（沿 +x，枪头在远端） |

> `arm_l/arm_r` 共用同一绘制函数，`hand_l/hand_r`、`leg_*`、`foot_*` 同理——**左右部件共用绘制**，所以画"一侧"即可，会镜像复用。

### 3.5 绘制原语 API（`src/art/vector.ts`，全部可选）

```ts
fillRect(ctx, x, y, w, h, style?)
fillRoundRect(ctx, x, y, w, h, radius, style?)
fillCircle(ctx, cx, cy, r, style?)
fillEllipse(ctx, cx, cy, rx, ry, style?)
fillPoly(ctx, pts: [x,y][], style?)        // 闭合多边形
fillQuadCurve(ctx, pts: [x,y][], style?)   // 二次贝塞尔闭合路径（奇数点=控制点）
strokeLine(ctx, x0,y0,x1,y1, color, width)
linearGradient(ctx, x0,y0,x1,y1, [[offset,color],...]) // 返回 CanvasGradient
// style = { fill?: string|CanvasGradient, stroke?: string, lineWidth?: number }
```

Canvas2D 原生 API（`ctx.arc`、`ctx.ellipse`、`ctx.save/restore`、`ctx.translate/rotate` 等）**均可直接使用**——原语库只是方便，不是限制。

### 3.6 配色（蓝方钢甲，供整体统一）

| 用途 | 色值 |
|---|---|
| 主钢甲（亮/中/暗） | `#4d6f9e` / `#38537c` / `#263a58` |
| 四肢甲（亮/暗） | `#33466a` / `#22304a` |
| 金属高光 / 中 / 暗 | `#aab6c2` / `#7c8896` / `#55616f` |
| 皮肤 | `#e8c39e` |
| 木（枪杆） | `#9a7a49` / `#7d5f36` / `#5f4726` |
| 皮革（腰带/靴） | `#4a3826`、`#8a6a3f` |
| 红（盔缨/枪缨） | `#b3302e`（可用 `#8f2422` 做暗部） |
| 描边 | `rgba(8,12,18,0.55)`，四肢 1px、铠甲 1.2px |

---

## 4. 现状诊断（为什么像"四根筷子"）

当前代码见 `src/art/parts/spearman.ts`（已在上文摘录关键参数）。问题清单：

1. **手臂 = 等宽圆角矩形**：`fillRoundRect(-3.2, 0, 6.4, 12)`，全程 6.4px 宽，无粗细渐变、无肘关节、无明暗。
2. **手 = 小矩形 + 圆点**：5.2px 宽矩形末端加 2.5px 圆，像棍端木塞。
3. **腿 = 近直筒梯形**：`3.6→3.2px`，锥度几乎为零；膝部只有一个小椭圆"护膝"，没有大腿/小腿的粗细差与转折。
4. **躯干 = 一块梯形 + 一根中线**：无胸甲弧面、无腹甲/裙甲分层，肩甲是两块小圆角矩形，整体扁平。
5. **头 = 圆 + 半圆盔 + 单片缨**：无五官、无面甲/护耳层次，盔缨单片无厚度。
6. **整体**：四肢与躯干几乎等粗、色彩单一渐变、描边细而统一 → 剪影感弱、像火柴人。

---

## 5. 目标规格（按部件重绘要求）

### 5.0 全局原则（每条都必须体现）

- **体积感**：每个部件至少两层明暗 —— 主色渐变 + 亮部高光条/高光点 + 暗部阴影边（内侧或下部）。
- **关节感**：四肢必须有"上粗下细"的锥度变化，肘/膝用护具或形状转折表达。
- **层次感**：躯干至少分胸甲/腹甲（裙甲）两段；头部分脸/盔/缨三段。
- **描边统一**：外轮廓深色描边（1–1.2px）；内部细节线用浅色或同色系暗线。
- **留白**：形状距画布边缘至少 1–2px（防止烘焙后裁边），画布 `w/h` 随之增大。
- **动起来不穿帮**：参考动画幅度——腿摆动约 ±0.62rad（≈±35°）、手臂摆动 ±0.5rad；部件长度/宽度要覆盖摆动范围（例如腿从髋延伸 22px 到踝、画布高 ≥ 24）。

### 5.1 头 `head`（建议画布 20×22，向上画 14–16px；头骨在 y=0）

- 颈甲：2–3 片叠甲（颈在 y≈-2..0）。
- 脸：皮肤圆（半径 ≈ 5.5）+ 简单五官（2–3 笔：眉毛/眼线、嘴线），可加腮红阴影。
- 头盔：半球带**顶部棱线**（高光弧）+ **护耳/护颊**（两侧下延）+ 双层盔沿 + **面甲开缝**（垂直暗缝，露出鼻梁）。
- 盔缨：双层（红 + 深红），从盔顶向后上方翘（y 约 -13..-16）。
- 保持挂点 (0,0) 在颈，头部整体在负 y 区域；**头+盔总高控制在 14–16px**（现状 19px 偏大，是"Q 版大头"感的来源之一）。

### 5.2 躯干 `torso`（建议画布 26×22，向上延伸 18px 到颈）

- **肩甲**：左右两片圆润半球形（高光在顶部偏外），从躯干两侧伸出（宽出躯干 2–3px）。
- **胸甲**：弧形轮廓（顶部窄、腰腹宽出一点再收），**弧面高光**（顶部一条横向亮带 + 左侧高光），两侧暗部渐深；中央竖脊线用"亮线 + 两侧暗线"表达，而非单根白线。
- **腹甲/裙甲**：腰带以下 3–4 片叠甲（错落的梯形/叶片形，交替深浅）。
- **腰带**：宽 4px 皮革 + 中央金属扣（亮金属 + 暗边），皮带有投影线。
- 顶部（y=-18）即肩线，与头骨/肩甲衔接处画一层"护颈甲"。

### 5.3 上臂 `arm_l/arm_r`（建议画布 14×18，向下 12px）

- 形状：**肩端宽（约 ±4.5px）→ 肘端窄（约 ±3.2px）**的锥形圆角。
- 明暗：外侧一条高光竖条（亮甲色），内侧一条暗影；肩垫为球形高光。
- 肘端：一圈护腕/肘甲（略凸出的环）。

### 5.4 前臂+手 `hand_l/hand_r`（建议画布 12×16，向下 12px）

- 前臂：肘端 ±2.8px → 腕端 ±2.2px 的锥形，颜色比上臂略深（前臂甲）。
- 腕部：护腕环（亮金属）。
- 手：**握拳的手**（约 5×5px 圆润方块，皮肤色 + 手背亮部 + 指缝 1–2 笔暗线），微握枪姿势（握拳朝内即可，枪由 `weapon` 部件表现）。

### 5.5 腿 `leg_l/leg_r`（建议画布 14×24，向下延伸 **22px** 到踝）

> 注意：髋→踝的骨骼距离是 **22px**（`foot` 骨挂在腿末端）。腿部件必须画满 22px，否则腿与靴子之间会出现缝隙/脱节（当前实现的腿只画了约 14–18px，是观感粗糙的原因之一）。

- 大腿：髋端 ±5px → 膝端 ±3.8px；**膝部大护膝**（半圆亮面，覆盖膝骨，宽出腿部 1–2px，位于局部 y≈12）。
- 小腿：膝端 ±3.2px → 踝端 ±2.6px，比大腿暗一档。
- 明暗：外侧高光条贯穿大腿→小腿（膝部断开），内侧暗部；腿部可加 1–2 条横向绑带/缝线细节。

### 5.6 脚 `foot_l/foot_r`（建议画布 14×9，向下 5px）

- 靴子：**圆头微翘**的靴形（踝在 (0,0)，靴口 4px 宽，鞋头向 +x 伸出约 5px、向 -x 约 2px）。
- 靴筒：一层叠口（亮边）；鞋底：暗色 1px 厚底。
- 脚底最下沿落在 y≈5（即地面 y=0 处为踝，脚到 y=5）。

### 5.7 长枪 `weapon`（建议画布 66×14，沿 +x 从挂点伸出）

- 枪杆：总长约 46px，**圆柱感**——顶部一条 1px 高光亮线、中部主色、底部 1px 暗影线（代替当前纯渐变平面）。
- 枪缨（红）：双层（亮红 + 暗红），比当前略大、带飘动弧度（可用 `fillQuadCurve`）。
- 枪头：菱形加宽（约 14px 长、±5px 宽），**中脊棱线**（亮线）+ 两侧暗面，枪尖锐利。
- 枪杆末端（挂点侧）加"枪尾锥"（金属小圆锥），让握持处有重量感。

### 5.8 比例参考（侧视 3/4 战棋风，总高约 72–77px，y 为世界坐标）

```
           ██              ← 盔顶 y≈-72..-74
          ████
           ████            ← 头+盔（y≈-58..-74，约 15px）
          ████████
        ████████████       ← 肩甲（y≈-52..-46，肩宽 ≥ 14px）
        ████████████
          ████████         ← 胸甲（y≈-40..-28，弧面+高光+脊线）
           ██████
          ██████████       ← 腹甲/裙甲（y≈-26..-20，3–4 片叠甲）
           ██████
          ████████         ← 腰带（y≈-24 附近，金属扣）
         ████  ████        ← 大腿（y≈-22..-10，粗 ±5px）
        ████    ████
         ████  ████        ← 膝部大护膝（y≈-10..-5）
        ███      ███       ← 小腿（y≈-5..0，细 ±2.6px）
       ████      ████      ← 靴（y≈0..5，圆头微翘）
```

（右臂持枪、左臂在身前；枪沿 +x 从右手伸出，枪头在身体前方约 2 个身位——枪长 46px 即如此。）

---

## 6. 姿态微调（默认禁止，确需时按此规则）

- 骨骼层级、骨骼名、挂点坐标、层序**一律不许改**。
- 若你觉得"持枪姿势太僵"而想调 rest 姿态（如 arms/weapon 的 rest `rotate`），仅可修改 `src/art/rig.ts` 中对应骨骼的 `transform.rotate` **数值**（幅度 ±0.3 以内），并在交付说明中列出。
- **注意**：动画关键帧是"绝对变换"，播放时会覆盖 rest；改动 rest 只影响无动画时间线的骨骼。改完必须验证 7 个动作都不穿帮（§7），若某动作明显异常，需在交付说明中说明原因，**不要**擅自改 `generate.ts` 的动画数据。

---

## 7. 验证方式

环境：Node ≥ 18，仓库根目录执行：

```bash
npm install          # 首次
npm run dev          # 启动开发服务器 → 浏览器打开 http://localhost:5173/
```

打开页面后：

1. **逐动作查看**：右上角调试面板 →"动画状态"区点 待机/移动/攻击/格挡/受击/死亡/转向，观察每个动作下的形象。
2. **看细节**：面板"镜头"区放大/缩小；页面滚轮缩放、拖拽/方向键平移。
3. **自动对打演示**：单位沿路巡逻，每约 4.5s 自动攻击（命中帧有闪白/顿帧/飘字/火花），死亡有溶解——这些特效会暴露部件穿帮。

自动化检查：

```bash
npx tsc --noEmit                       # 类型检查必须 0 错误
npm test                               # 31 个单元测试必须全过
node scripts/verify-browser.mjs        # 无头浏览器 E2E（需 dev server 运行中）：
                                       # 7 动作播放、像素级渲染、无 console 错误
```

---

## 8. 验收标准（DoD，全部满足才算完成）

1. **无"筷子感"**：四肢有明确粗细变化（最窄处 ≥ 2.2px）与肘/膝关节表达；躯干分胸甲/腹甲/裙甲多层；头部 ≥ 3 层（脸/盔/缨）且有五官。
2. **立体感**：每个部件有 ≥ 2 层明暗（高光 + 暗部），金属部件有高光条/高光点。
3. **比例协调**：总高约 72–77px（±4px），下肢（腿+靴）≈ 27px、躯干 ≈ 18–20px、头+盔 ≈ 14–16px，肩宽 ≥ 14px，四肢与躯干有明确粗细对比；头不再明显偏大。
4. **约定保持**：挂点 (0,0)、y 向下、displayIndex 层序、slot 名/部件名不变；画布尺寸改动已同步到 `PartDef` 的 `w/h`。
5. **运行正确**：`tsc` 0 错误、`npm test` 全过、`verify-browser` 全绿、页面无 console 错误；7 个动作播放正常，攻击/格挡/死亡特效下部件不穿帮（无残缺、无飞出残影）。
6. **范围合规**：未改动 §2 禁止清单中的任何文件；如动了 `rig.ts`，交付说明已逐条列出。

---

## 9. 交付物

1. 修改后的 `src/art/parts/spearman.ts`（主交付，附简要的"改了什么、为什么"说明）；
2. 若新增了原语：`src/art/vector.ts` 的增量；
3. 若微调了姿态：`src/art/rig.ts` 的改动列表；
4. 自测结果（§7 各项的通过情况）；
5. 可选：改动前后的对比截图。

---

*本文档由主项目方编写，作为长枪兵形象绘制外包的独立任务书；与 `需求文档.md` 冲突时以需求文档为准（涉及需求 7.1 矢量部件原则、7.4 形象草案、9.1 动作清单）。*
