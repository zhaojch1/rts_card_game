# 长枪兵格挡后攻击修复测试说明

## 问题描述
长枪兵被格挡复位后，重新准备攻击时会"往上戳空气"，应该重新摆平长枪发动攻击。

## 修复方案
在`approachTarget`函数中，对于长枪兵，修改了到位判断条件：
- **原逻辑**：只要X轴距离合适（`pastIdeal`为true）就认为到位
- **新逻辑**：需要同时满足X轴距离合适（`pastIdeal`）且Y轴对齐良好（`dy < 6`）

## 技术细节
1. **问题根源**：当长枪兵从`recover`状态切换到`approach`状态时，如果已经在"合适距离"但Y轴距离较远，会直接进入`level`状态，导致武器指向错误方向
2. **修复位置**：`src/DemoScene.ts` 第630-644行，`approachTarget`函数
3. **修复逻辑**：长枪兵必须同时满足以下条件才认为到位：
   - `pastIdeal`：X轴距离合适（已经比理想距离更近）
   - `dy < 6`：Y轴对齐良好（与精确对齐条件一致）

## 测试步骤
1. 启动开发服务器：`npm run dev`
2. 访问 http://localhost:5173
3. 召唤一个长枪兵和一个剑士
4. 让长枪兵攻击剑士
5. 观察剑士格挡后长枪兵的行为：
   - **修复前**：长枪兵可能会在Y轴距离较远时直接进入攻击状态，导致"往上戳空气"
   - **修复后**：长枪兵会先调整Y轴位置，确保对齐良好（dy < 6）后再进入攻击状态

## 预期结果
- 长枪兵被格挡后，会先调整位置，确保与敌人Y轴对齐良好
- 然后重新摆平长枪发动攻击，不再出现"往上戳空气"的情况
- 贴脸逻辑保持不变：敌人贴脸时要么撤退要么不动

## 注意事项
1. 这个修复只影响长枪兵的`approachTarget`函数
2. 贴脸逻辑（`spearTooClose`判定）在`approachTarget`之前执行，不受影响
3. 剑士的行为不受影响
4. 构建验证：`npm run build` 成功通过，无语法错误

## 相关代码
```typescript
// 修复前：
if (pastIdeal) return true;

// 修复后：
if (pastIdeal && dy < 6) return true;
```