{
  "name": "rts-card-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "卡牌 RTS 战棋游戏（网页端）· 阶段 0 地基",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "node scripts/verify-browser.mjs"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "puppeteer-core": "^25.8.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
